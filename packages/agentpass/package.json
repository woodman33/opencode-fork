{
  "name": "@opencode-ai/agentpass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AgentPass — authentication, API key management, and rate limiting for Continuum",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "^3.0.0"
  }
}
